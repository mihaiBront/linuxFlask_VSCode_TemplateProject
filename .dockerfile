# PENDING IMPLEMENTATION

# 1. Create Ubuntu image
# 2. Install Python > 3.12
# 3. Copy necessary files to a directory inside the docker
# 4. Run .scripts/env-startup.bash to get the virtual environment ready
# 5. Make script to run app.py (product) at startup.